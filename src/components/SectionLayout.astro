---
interface Props { id: string; title: string; }
const { id, title } = Astro.props;
---
<section id={id} class="border-t border-[--color-line] px-7 py-9 md:px-9 md:py-12">
  <h2 class="mb-5 text-[11px] font-medium tracking-[0.2em] text-[--color-muted]">{title}</h2>
  <slot />
</section>
