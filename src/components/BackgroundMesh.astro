<div class="mesh" aria-hidden="true">
  <div class="shimmer"></div>
  <div class="blob b1"></div>
  <div class="blob b2"></div>
</div>

<style>
  .mesh {
    position: absolute; inset: 0; overflow: hidden; z-index: 0;
    background: linear-gradient(120deg, #070e1b, #0f2138, #17324f, #0f2138, #070e1b);
    background-size: 320% 320%;
    animation: gm_drift 12s ease infinite;
  }
  .shimmer {
    position: absolute; inset: -40%;
    background: conic-gradient(from 0deg, transparent 0deg, rgba(120,170,230,0.16) 90deg, transparent 180deg, rgba(90,140,210,0.14) 270deg, transparent 360deg);
    animation: gm_spin 22s linear infinite;
  }
  .blob { position: absolute; border-radius: 50%; filter: blur(46px); will-change: transform; }
  .b1 { width: 220px; height: 220px; background: #4f86c6; opacity: 0.32; left: 8%; top: -25%; animation: gm_float1 15s ease-in-out infinite; }
  .b2 { width: 200px; height: 200px; background: #7fb0e6; opacity: 0.22; right: 5%; bottom: -30%; animation: gm_float2 19s ease-in-out infinite; }
  @keyframes gm_drift { 0% { background-position: 0% 50%; } 50% { background-position: 100% 50%; } 100% { background-position: 0% 50%; } }
  @keyframes gm_spin { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }
  @keyframes gm_float1 { 0% { transform: translate(-15%,-10%) scale(1); } 33% { transform: translate(20%,15%) scale(1.3); } 66% { transform: translate(10%,-15%) scale(1.1); } 100% { transform: translate(-15%,-10%) scale(1); } }
  @keyframes gm_float2 { 0% { transform: translate(20%,20%) scale(1.2); } 50% { transform: translate(-20%,-5%) scale(1); } 100% { transform: translate(20%,20%) scale(1.2); } }
  @media (prefers-reduced-motion: reduce) {
    .mesh, .shimmer, .blob { animation: none !important; }
  }
</style>
